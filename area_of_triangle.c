#include <stdio.h>

int main()
{
	float l,w,area;
	printf("enter the length of triangle :");
	scanf("%f",&l);
	printf("enter the width of triangle :");
	scanf("%f",&w);
	area = (1.0/2.0)*l*w;
	printf("\nthe area of triangle is : %f",area);
	

 
}
